#!/usr/bin/env python
"""Make a lightcurve"""
import sys
import enrico.orbitalLC  as orb
from enrico.config import get_config

try:
    infile = sys.argv[1]
except:
    print('FATAL: Config file not found.')
    sys.exit(1)

config = get_config(infile)
orbLC = orb.OrbitalLC(config)
orbLC.MakeLC()
