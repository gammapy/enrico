#!/usr/bin/env python
"""Produce a TS map"""
import sys
from enrico.tsmap import runTSMap

try:
    infile = sys.argv[1]
except:
    print('FATAL: Config file not found.')
    sys.exit(1)

runTSMap(infile)

