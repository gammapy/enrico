#!/usr/bin/env python
"""Make a lightcurve"""
import sys
import enrico.lightcurve  as lc

try:
    infile = sys.argv[1]
except:
    print('FATAL: Config file not found.')
    sys.exit(1)

lc = lc.LightCurve(infile)
lc.MakeLC()
